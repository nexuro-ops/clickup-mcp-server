# Integration Tests Configuration
# ================================
# This file shows example values for integration test environment variables.
# For REAL integration testing, create a .env.integration file with actual ClickUp API token.
# NEVER commit the actual .env.integration file - it's in .gitignore for security.

# ClickUp Personal API Token for Integration Tests
# Use a test workspace token, NOT production tokens
# Get your token from: https://app.clickup.com/settings/integrations/api
CLICKUP_PERSONAL_TOKEN_INTEGRATION=your_test_api_token_here

# Optional: Specific workspace ID for integration tests
# If not provided, uses workspace from the API token
CLICKUP_TEST_WORKSPACE_ID=

# Enable verbose output for integration tests
# Useful for debugging test failures
INTEGRATION_VERBOSE=false
